import { DefaultTheme } from 'styled-components';

import { colors } from './colors';
import { fonts } from './fonts';

const veganAppTheme: DefaultTheme = {
  colors: colors,
  fonts: fonts,
};

export { veganAppTheme };
